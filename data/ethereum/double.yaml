project: default
chain: ethereum

env:
  local:
    networkid: 999
    backend: geth
    datadir: ~/.boson/datadir
    hosts:
      - 127.0.0.1:30303
  test:
    keydir: ~/.boson/default/keys
    # hosts:
      # - remoteIP:port
  main:
    # keydir: ~/.boson/default/keys
    # hosts:
      # - remoteIP:port
    {}
