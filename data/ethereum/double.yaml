project: default
chain: ethereum

env:
  local:
    networkid: 999
    backend: geth
    datadir: ~/.double/datadir
    hosts:
      - 127.0.0.1:30303
  test:
    keydir: ~/.double/default/keys
    # hosts:
      # - remoteIP:port
  main:
    # keydir: ~/.double/default/keys
    # hosts:
      # - remoteIP:port
    {}
